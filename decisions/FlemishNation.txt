country_decisions = {
	form_greater_flanders = {
		major = yes
		potential = {
			primary_culture = flemish
			NOT = {exists = FLN}
		}
		
		allow = {
			adm_tech = 10

			owns_all_provinces = {
				culture = flemish
			}
		}

		effect = {
			change_tag = FLN
			set_capital = 1744

			if = {
				limit = { NOT = { government_rank = 2 } }
				set_government_rank = 2
			}

			if = {
				limit = {
					has_custom_ideas = no
				}
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
		}
	}
}