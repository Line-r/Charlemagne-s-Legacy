namespace = incident_lowlands

country_event = {
	id = incident_lowlands.1
	title = incident_lowlands.1.t
	desc = incident_lowlands.1.d
	picture = CITY_VIEW_eventPicture

	mean_time_to_happen = {
		days = 1
	}
	fire_only_once = yes

	trigger = {
		ROOT = {
			AND = {
				OR = {
					primary_culture = flemish
					primary_culture = dutch
					primary_culture = wallonian
					primary_culture = frisian
				}
				grown_by_development = 25
			}
		
		}
	}

	option = {
		name = "incident_lowlands.1.a"
		emperor = {
			set_imperial_incident = incident_balance_lowlands
		}
	}
}

country_event = {
	id = incident_lowlands.2
	title = incident_lowlands.2.t
	desc = incident_lowlands.2.d
	picture = CITY_VIEW_eventPicture

	is_triggered_only = yes

	option = {
		name = "incident_lowlands.2.a"
		hidden_effect = {
			set_global_flag = lowlands_battle_royale
		}
	}

	option = {
		name = "incident_lowlands.2.b"
		hidden_effect = {
			set_global_flag = lowlands_battle_royale_prevented
		}
	}
}